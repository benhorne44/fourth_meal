<h1><%= @restaurant.name %></h1>
<div id="items">
  <% @items.each do |item| %>
    <div id="item_<%= item.id %>">
      <%= item.title %>
      <%= link_to "Add to Order", add_item_path(item.id), method: "post" %>
    </div>
  <% end %>
</div>
