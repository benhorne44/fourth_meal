<!DOCTYPE html>
<html>
 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Dinner Dash" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Dinner Dash" %>">
     <link href='http://fonts.googleapis.com/css?family=Merriweather+Sans:400,300,700,400italic' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag "application", :media => "all" %>
   <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div id="main_logo">
          <%= link_to image_tag("retto_logo-rgb.png"), root_path, alt: 'retto' %>
        </div>
          <div class="controls">
            <p><%= login_status %></p>
            <%= link_to "Order", cart_path %>
            <% if current_user %>
            <%= link_to "My Account", user_path(current_user), {:class => "btn btn-info"} %>
              <%= link_to "Logout", logout_path, {:class => "btn btn-warning"} %>
            <% else %>
              <%= link_to "Login", login_path, {:class => "btn btn-primary login"} %>
            <% end %>
          </div>
        <div class="container">
        </div>
      </nav>
    </header>
    <main role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
            <div class="container">
              <p>Retto. An open-source food ordering service built by gSchool class[1]</p>
              <p>Will Mitchell, Ben Horne, &amp; Louisa Barrett</p>
              <%= link_to "I want to create a restaurant", new_restaurant_path %>
            </div>
          </footer>
        </div>
      </div>
    </main>
  </body>
</html>
