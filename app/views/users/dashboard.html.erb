<div id ="dashboard">
  <h2>Admin Dashboard</h2>

  <div class="categories-list">
    <h3>Categories</h3>
    <a id="categories-list"></a>
    <%= link_to "Create New Category", new_category_path, {:class => "btn btn-success"} %>
    <br>
    <br>
    <table>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to "Edit", edit_category_path(category.id), {:class=> "btn btn-warning edit-category" } %></td>
      </tr>
      <% end %>
  </div>
  </table>

  <div class="orders-list">
    <h3>Orders</h3>
    <a id="orders-list"></a>
    <%= link_to "Order History", orders_path %>
  </div>

  <div class="users-list">
    <h3>Users</h3>
    <a id="users-list"></a>
    <table>
     <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user_path(user.id) %></td>
      </tr>
     <% end %>
    </table>
  </div>

  <div id='restaurants'>
    <h3>Restaurants</h3>
    <% @restaurants.each do |restaurant| %>
      <%= link_to restaurant.name, restaurant_dashboard_path(restaurant) %>
    <% end %>
  </div>

</div>
